# 커뮤니티아트랩 발견 — 홈페이지 저장소

artlabbalgyeon.kr 의 소스 저장소입니다. **main 브랜치에 push하면 1~2분 안에 실제 사이트에 반영됩니다** (GitHub Pages). 이 저장소가 유일한 원본입니다 — 다른 곳의 사본을 참조하지 마세요.

## 파일 구조

- `index.html` — 홈(메인 페이지). 소개, 세 갈래 활동, 대표 프로젝트 6개, 교구와 책, 소식, 문의 폼.
- `projects.html` — 전체 프로젝트 목록(필터 + 모달). **index.html과 별개 파일**이므로 새 프로젝트를 추가할 땐 두 파일 모두 확인할 것 (홈에는 대표 6개만).
- `map.html` — 「발견의 지도」. 장소 데이터는 **파일 상단의 `PLACES` 배열만 고치면 됨**. 새 장소의 좌표가 필요하면 Nominatim(무료, User-Agent 필수, 요청 간 1.2초 간격)으로 조회.
- `thanks.html` — 문의 폼 제출 후 이동하는 감사 페이지.
- `CNAME` — 도메인 연결 파일. **절대 수정·삭제 금지** (지우면 artlabbalgyeon.kr 접속이 끊어짐).
- `favicon.svg`, `og.png`, `robots.txt`, `sitemap.xml` — 아이콘·미리보기·검색엔진용. 새 페이지를 추가하면 sitemap.xml에도 넣어줄 것.

## 브랜드 규칙 (반드시 지킬 것)

- 이름 표기는 **"커뮤니티아트랩 발견"** — 커뮤니티아트랩은 붙여 쓰고, 발견 앞에만 공백. "아트랩 발견"으로 줄여 쓰지 말 것.
- 영문 표기: COMMUNITY ARTLAB BALGYEON (ARTLAB 붙여쓰기).
- 포인트 컬러: 일렉트릭 블루/인터내셔널 클라인 블루 계열 `#002FA7`. 로고의 빈 원 색만 맥락에 따라 바꿀 수 있고(유동 시스템), 기본은 먹색 `#1A1A1A`.
- 디자인 방향: 흰 바탕 + 흑백 대비 + Black Han Sans 초대형 타이포 + 3px 괘선 + 검정 반전 섹션 (MCA 시카고풍 볼드 뮤지엄). 이 톤을 벗어나는 장식을 추가하지 말 것.

## 자주 하는 작업

- **문구·소식 수정**: 해당 HTML에서 텍스트만 교체 → 커밋 → push.
- **프로젝트 추가**: projects.html의 카드 + 모달에 추가, 대표 프로젝트로 올릴 거면 index.html에도. 필터 분류 확인.
- **지도에 장소 추가**: map.html의 PLACES 배열에 항목 추가 (이름, 좌표, 분야 색, 프로젝트 목록).
- **사진 추가**: 이미지 파일을 저장소에 넣고(웹용으로 200KB 내외 권장, 필요시 리사이즈) `<img>`로 참조.
- **문의 폼**: FormSubmit으로 artlab.balgyeon@gmail.com에 전달됨. 폼 구조를 바꾸면 실제 제출 테스트까지 할 것.

## 작업 수칙

- 수정 후에는 반드시 **커밋 + push**까지 해야 사이트에 반영됨. push 전에 변경 내용을 사용자(운영자)에게 한 줄로 요약해 확인받기.
- 큰 변경 전에는 현재 상태가 git 히스토리에 있으므로 되돌리기 요청("어제 상태로 되돌려줘")에는 `git revert`/이전 커밋 복원으로 대응.
- 커밋 메시지는 한국어로 간단히 (예: "9월 프로그램 모집 배너 교체").
